<!DOCTYPE html>
<html>
  <% include ../../partials/head %>
  <body>
    <div>

      <% include ../../partials/nav %>

      <div class="container">
        <div class="jumbotron">
          <h2>Preview Your Ad</h2>

            <form action="/ad/demo" method="get">
              <div class="form-group">
                <label for="title">Ad Title</label>
                <input type="text" class="form-control" id="title" name="title" placeholder="Find Aliens with Bitcoin" value="<%= ad.title %>">
              </div>
              <div class="form-group">
                <label for="description">Ad Description</label>
                <textarea class="form-control" id="description" rows="3" name="description" placeholder="AlienSearchE16 is a bitcoin payable web app designed for the 21 Marketplace to allow clients to pay for the server to run SETI@home."><%= ad.description %></textarea>
              </div>
              <div class="form-group">
                <label for="imageUrl">Ad Image Url</label>
                <input type="text" class="form-control" id="imageUrl" name="imageUrl" value="<%= ad.imageUrl %>" placeholder="https://www.esixteen.co/img/network.png">
              </div>
              <div class="form-group">
                <label for="targetUrl">Ad Target Url</label>
                <input type="text" class="form-control" id="targetUrl" name="targetUrl" value="<%= ad.targetUrl %>" placeholder="https://www.esixteen.co/apps/aliensearche16">
              </div>
              <button type="submit" class="btn btn-default">Update</button>
            </form>


          <div class="" style="text-align: center;">
            <h2>Demo Ad</h2>
            <iframe src="/ad/preview?title=<%= ad.title %>&description=<%= ad.description %>&imageUrl=<%= ad.imageUrl %>&targetUrl=<%= ad.targetUrl %>" width="500" frameborder="1" ></iframe>
          </div>
        </div>
      </div>


    </div>
  </body>
</html>
